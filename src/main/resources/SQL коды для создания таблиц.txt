
-- для DAO Authentication!
-- создание таблиц для автоматической записи юзеров и их ролей в БД!
create table if not exists users
(
    id       int auto_increment,
    username varchar(30) not null,
    password varchar(80),
    email    varchar(50) unique,
    primary key (id)
    );
create table if not exists  roles
(
    id   int auto_increment,
    name varchar(50) not null,
    primary key (id)
);
create table if not exists  users_roles
(
    user_id int not null,
    role_id int    not null,
    primary key (user_id, role_id),
    foreign key (user_id) references users (id),
    foreign key (role_id) references roles (id)
);

-- Добавляю названия ролей в талицу Roles
insert into roles (name)
values ('ROLE_ADMIN'),
       ('ROLE_USER');

-- добавляю юзера (это пароль "admin" через BCrypt) https://www.browserling.com/tools/bcrypt
insert into users (username, password, email)
values ('admin', '$2a$10$ToAcqTCNCyJzDhJfZKvDPuYvraaZfSkc7r31oSwkegGI5B1LZfKRq', 'admin@gmail.com');

-- добавляю роли юзеру (юзеру с id=1, даю роль  с id=1):
insert into users_roles (user_id, role_id) values (1, 1);


